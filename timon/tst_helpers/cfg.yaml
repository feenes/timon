type: timon config
version: '0.1'
hosts: # list of all hosts to be monitored and their config
  local: {addr: 127.0.0.1, hostname: localhost,
          probes: [
          ]}

probes: {}

schedules: {}

default_params:
    nothing: 0
